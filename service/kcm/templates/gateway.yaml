{{- if .Values.kcm.gateway }}
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kcm-gateway
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kcm.labels" . | nindent 4 }}
spec:
  interval: 10m0s
  releaseName: kcm-gateway
  targetNamespace: {{ .Release.Namespace }}
  timeout: 15m0s
  {{- if or .Values.flux.enabled .Values.gwapi.enabled .Values.certmanager.enabled .Values.kcm.issuer }}
  dependsOn:
  {{- if .Values.flux.enabled }}
    - name: flux
  {{- end }}
  {{- if .Values.gwapi.enabled }}
    - name: gateway-api
  {{- end }}
  {{- if .Values.certmanager.enabled }}
    - name: cert-manager
  {{- end }}
  {{- if .Values.kcm.issuer }}
    - name: kcm-issuer
  {{- end }}
  {{- end }}
  chart:
    metadata:
      labels:
        k0rdent.mirantis.com/managed: "true"
    spec:
      chart: gateway
      version: 1.0.3
      sourceRef:
        kind: HelmRepository
        name: labsonline-catalog
  install:
    crds: CreateReplace
    remediation:
      remediateLastFailure: true
      retries: 3
  upgrade:
    crds: CreateReplace
  # TODO: images
  # postRenderers:
  #   - kustomize:
  #       images:
  #         - name: ""
  #           newName: ""
  #           newTag: ""
  {{- with .Values.kcm.gateway }}
  values:
    gateways:
    - {{ toYaml . | nindent 6 }}
  {{- end }}
{{- end }}
