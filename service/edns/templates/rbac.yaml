{{- if .Values.endpoints }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-dns-resource-creator
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "edns.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: pre-install,pre-upgrade
    helm.sh/hook-weight: "-5"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: external-dns-resource-creator
  labels:
    {{- include "edns.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: pre-install,pre-upgrade
    helm.sh/hook-weight: "-5"
rules:
  - apiGroups:
      - externaldns.k8s.io
    resources:
      - dnsendpoints
    verbs:
      - create
      - delete
      - get
      - list
      - patch
      - update
  - apiGroups:
      - apiextensions.k8s.io
    resources:
      - customresourcedefinitions
    verbs:
      - get
      - list
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: external-dns-resource-creator
  labels:
    {{- include "edns.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: pre-install,pre-upgrade
    helm.sh/hook-weight: "-5"
subjects:
  - kind: ServiceAccount
    name: external-dns-resource-creator
    namespace: {{ .Release.Namespace }}
roleRef:
  kind: ClusterRole
  name: external-dns-resource-creator
  apiGroup: rbac.authorization.k8s.io
{{- end }}
