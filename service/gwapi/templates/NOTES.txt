{{- $envoyEnabled := .Values.envoy.enabled }}
{{- $nginxEnabled := .Values.nginx.enabled }}
{{- $traefikEnabled := .Values.traefik.enabled }}
{{- $enabledProviders := list }}
{{- if $envoyEnabled }}{{ $enabledProviders = append $enabledProviders "Envoy Gateway" }}{{ end }}
{{- if $nginxEnabled }}{{ $enabledProviders = append $enabledProviders "NGINX Gateway Fabric" }}{{ end }}
{{- if $traefikEnabled }}{{ $enabledProviders = append $enabledProviders "Traefik Gateway" }}{{ end }}

🎉 Gateway API has been successfully deployed!

📋 DEPLOYMENT SUMMARY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Release Name:      {{ .Release.Name }}
Namespace:         {{ .Release.Namespace }}
{{- if $enabledProviders }}
Enabled Providers: {{ join ", " $enabledProviders }}
{{- else }}
⚠️  WARNING: No gateway providers are enabled!
{{- end }}

🔧 VERIFICATION STEPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Check gateway classes:
   kubectl get gatewayclass

{{- if $envoyEnabled }}
2. Verify Envoy Gateway components:
   kubectl get pods -n envoy-gateway-system
{{- end }}

{{- if $nginxEnabled }}
2. Verify NGINX Gateway Fabric:
   kubectl get pods -l app.kubernetes.io/name=nginx-gateway-fabric
{{- end }}

{{- if $traefikEnabled }}
2. Verify Traefik Gateway:
   kubectl get pods -l app.kubernetes.io/name=traefik
{{- end }}

3. Create your first Gateway:
   kubectl apply -f - <<EOF
   apiVersion: gateway.networking.k8s.io/v1
   kind: Gateway
   metadata:
     name: my-gateway
     namespace: {{ .Release.Namespace }}
   spec:
     gatewayClassName: {{ if $envoyEnabled }}envoy{{ else if $nginxEnabled }}{{ .Values.nginx.gatewayClassName }}{{ else if $traefikEnabled }}traefik{{ end }}
     listeners:
     - name: http
       port: 80
       protocol: HTTP
   EOF

🚀 NEXT STEPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{{- if $enabledProviders }}
• Create HTTPRoute resources to route traffic to your services
• Configure TLS certificates for secure connections
• Set up monitoring and observability for your gateways
{{- else }}
• Enable at least one gateway provider in your values.yaml:
  envoy.enabled: true    # For Envoy Gateway
  nginx.enabled: true    # For NGINX Gateway Fabric
  traefik.enabled: true  # For Traefik Gateway
{{- end }}

📚 DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{{- if $envoyEnabled }}
• Envoy Gateway: https://gateway.envoyproxy.io/docs/
{{- end }}
{{- if $nginxEnabled }}
• NGINX Gateway Fabric: https://docs.nginx.com/nginx-gateway-fabric/
{{- end }}
{{- if $traefikEnabled }}
• Traefik Gateway: https://doc.traefik.io/traefik/providers/kubernetes-gateway/
{{- end }}
• Gateway API: https://gateway-api.sigs.k8s.io/

💡 For troubleshooting and support, check the documentation links above.
