{{- include "gwapi.validateCrdsInstall" . -}}
{{- /* All CRDs and related resources (e.g. ValidatingAdmissionPolicy) are managed via the
   gwapi-crds-standard / gwapi-crds-experimental embedded subcharts (Helm-native crds/
   directories), not rendered here. This file only enforces mutual exclusivity above. */ -}}
