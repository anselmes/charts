{{- if and .Values.adminJob.enabled .Values.adminJob.serviceAccountName.create }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.adminJob.serviceAccountName.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aistor.labels" $ | nindent 4 }}
{{- end }}
