---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vault-ca-issuer
  namespace: ucp
spec:
  interval: 10m0s
  releaseName: vault-ca-issuer
  timeout: 15m0s
  dependsOn:
    - name: flux
      namespace: sre
    - name: cert-manager
      namespace: cert-manager
  chart:
    spec:
      interval: 5m
      chart: ca
      version: 1.0.4
      sourceRef:
        kind: HelmRepository
        name: labsonline-catalog
        namespace: sre
  install:
    crds: CreateReplace
    remediation:
      remediateLastFailure: true
      retries: 3
  upgrade:
    crds: CreateReplace
  values:
    issuers:
      - name: vault-ca-issuer
        selfSigned: true
        secret:
          create: false
    certificates:
      - name: vault
        dns:
          name: vault.k8s.orb.local
          aliases:
            - vault.k8s.orb.local
            - vault.ucp.svc.cluster.local
            - vault.ucp.svc
            - vault
        issuerRef:
          name: vault-ca-issuer
          kind: Issuer
