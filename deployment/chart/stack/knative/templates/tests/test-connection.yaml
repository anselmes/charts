apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "knative.fullname" . }}-test-connection"
  labels:
    {{- include "knative.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "knative.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
