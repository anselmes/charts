apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "capi.fullname" . }}-test-connection"
  labels:
    {{- include "capi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "capi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
