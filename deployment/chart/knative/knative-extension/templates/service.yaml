apiVersion: v1
kind: Service
metadata:
  name: {{ include "knative-extension.fullname" . }}
  labels:
    {{- include "knative-extension.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "knative-extension.selectorLabels" . | nindent 4 }}
