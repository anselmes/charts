apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "csi-resources.fullname" . }}-test-connection"
  labels:
    {{- include "csi-resources.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "csi-resources.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
