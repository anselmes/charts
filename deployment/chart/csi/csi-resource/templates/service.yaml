apiVersion: v1
kind: Service
metadata:
  name: {{ include "csi-resources.fullname" . }}
  labels:
    {{- include "csi-resources.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "csi-resources.selectorLabels" . | nindent 4 }}
